<% flash.each do |type, message| %>
	<div class="row">
	  <div class="alert <%= bootstrap_class_for(type) %> fade in col-md-4 col-md-offset-4">
	    <button class="close" data-dismiss="alert">×</button>
	    <%= message %>
	  </div>
	</div>
<% end %>
